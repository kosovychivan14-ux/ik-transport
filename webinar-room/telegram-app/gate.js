const status=document.getElementById('status'),retry=document.getElementById('retry');
let busy=false;
async function enter(){if(busy)return;const telegram=window.Telegram?.WebApp;const initData=telegram?.initData;if(!initData){status.textContent='Відкрийте @ivankosovych_bot у Telegram і натисніть «Дивитися вебінар». Кімната відкривається лише як захищений Mini App.';return;}busy=true;retry.hidden=true;status.textContent='Перевіряємо ваш акаунт Telegram…';try{telegram.ready();telegram.expand();const response=await fetch('/api/telegram/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({initData}),credentials:'same-origin',signal:AbortSignal.timeout(15000)});if(!response.ok)throw new Error(response.status===403?'Не вдалося підтвердити вхід через Telegram. Закрийте вікно та відкрийте кнопку в боті ще раз.':'Сервер входу тимчасово недоступний. Спробуйте ще раз.');location.reload();}catch(error){status.textContent=error.message;retry.hidden=false;}finally{busy=false;}}
retry.addEventListener('click',enter);enter();
